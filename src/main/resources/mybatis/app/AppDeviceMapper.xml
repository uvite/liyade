<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.app.mapper.AppDeviceMapper">
    
    <resultMap type="AppDevice" id="AppDeviceResult">
        <result property="deviceId"    column="device_id"    />
        <result property="deviceName"    column="device_name"    />
        <result property="deviceType"    column="device_type"    />
        <result property="supplierId"    column="supplier_id"    />
        <result property="deviceStatus"    column="device_status"    />
        <result property="deviceCode"    column="device_code"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />

        <association property="supplier"    column="supplierId" javaType="AppSupplier" resultMap="supplierResult" />

    </resultMap>
    <resultMap id="supplierResult" type="AppSupplier">
        <id     property="supplierId"    column="supplier_id"     />
        <result property="supplierName"  column="supplier_name"   />
    </resultMap>

    <sql id="selectAppDeviceVo">
        select d.device_id, d.device_name, d.device_type, d.supplier_id, d.device_status, d.device_code, d.create_by, d.create_time, d.update_by,
               d.update_time, d.remark,s.supplier_id,s.supplier_name
        from app_device as d
            left join app_supplier s on d.supplier_id = s.supplier_id

    </sql>

    <select id="selectAppDeviceList" parameterType="AppDevice" resultMap="AppDeviceResult">
        <include refid="selectAppDeviceVo"/>
        <where>  
            <if test="deviceName != null  and deviceName != ''"> and d.device_name like concat('%', #{deviceName}, '%')</if>
            <if test="deviceType != null  and deviceType != ''"> and d.device_type = #{deviceType}</if>
            <if test="supplierId != null "> and d.supplier_id = #{supplierId}</if>
            <if test="deviceStatus != null  and deviceStatus != ''"> and d.device_status = #{deviceStatus}</if>
            <if test="deviceCode != null  and deviceCode != ''"> and d.device_code = #{deviceCode}</if>
        </where>
    </select>
    
    <select id="selectAppDeviceByDeviceId" parameterType="Long" resultMap="AppDeviceResult">
        <include refid="selectAppDeviceVo"/>
        where d.device_id = #{deviceId}
    </select>
        
    <insert id="insertAppDevice" parameterType="AppDevice" useGeneratedKeys="true" keyProperty="deviceId">
        insert into app_device
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="deviceName != null">device_name,</if>
            <if test="deviceType != null">device_type,</if>
            <if test="supplierId != null">supplier_id,</if>
            <if test="deviceStatus != null">device_status,</if>
            <if test="deviceCode != null">device_code,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="deviceName != null">#{deviceName},</if>
            <if test="deviceType != null">#{deviceType},</if>
            <if test="supplierId != null">#{supplierId},</if>
            <if test="deviceStatus != null">#{deviceStatus},</if>
            <if test="deviceCode != null">#{deviceCode},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateAppDevice" parameterType="AppDevice">
        update app_device
        <trim prefix="SET" suffixOverrides=",">
            <if test="deviceName != null">device_name = #{deviceName},</if>
            <if test="deviceType != null">device_type = #{deviceType},</if>
            <if test="supplierId != null">supplier_id = #{supplierId},</if>
            <if test="deviceStatus != null">device_status = #{deviceStatus},</if>
            <if test="deviceCode != null">device_code = #{deviceCode},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where device_id = #{deviceId}
    </update>

    <delete id="deleteAppDeviceByDeviceId" parameterType="Long">
        delete from app_device where device_id = #{deviceId}
    </delete>

    <delete id="deleteAppDeviceByDeviceIds" parameterType="String">
        delete from app_device where device_id in 
        <foreach item="deviceId" collection="array" open="(" separator="," close=")">
            #{deviceId}
        </foreach>
    </delete>
</mapper>